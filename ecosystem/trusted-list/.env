# Canonical public origin used in the curl commands shown on the site.
# Non-secret (VITE_* is exposed to the client bundle). Override per-deployment in Vercel's env settings.
VITE_SITE_URL=https://trusted-list.vercel.app
