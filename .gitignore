.gradle/
build/
.kotlin/
.idea/
*.iml
.DS_Store
swift/.build/
swift/.swiftpm/
ios/.build/
ios/.swiftpm/
local.properties
node_modules/
tools/headless-interop/package-lock.json

# NestJS wallet-provider build output
wallet-provider/dist/
wallet-provider/*.db
wallet-provider/*.db-*

# Demo reader-auth identity (private key) — provisioned per machine, never committed
demo/app/src/main/assets/reader_wrpac.json

# Xcode (demo-ios) — per-user state and build output, never committed
xcuserdata/
*.xcuserstate
DerivedData/

# iOS demo reader-auth WRPAC (local secret, mirrors android assets/reader_wrpac.json)
demo-ios/AxleWallet/AxleWallet/reader_wrpac.json

# iOS release / TestFlight — secrets & build artifacts, never committed (see demo-ios/RELEASE.md)
AuthKey_*.p8
*.xcarchive
*.ipa
demo-ios/**/ExportOptions.plist
demo-ios/fastlane/.env*
demo-ios/fastlane/report.xml
